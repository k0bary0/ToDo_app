<h1><%= @task.title %></h1>

<p><%= @task.description %></p>

<ul>
  <li><%= link_to "Edit", edit_task_path(@task) %></li>
  <li><%= link_to "destroy", user_path(current_user), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<div><%= link_to "back to list", user_path(current_user) %></div>